<configuration>
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <target>System.out</target>
    <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
      <level>INFO</level>
    </filter>
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <if condition='p("LOGSTASH_ENABLED").equals("true")'>
    <then>
      <appender name="LOGSTASH_EVENT" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <destination>${LOGSTASH_HOST:-logstash}:4560</destination>

        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
          <customFields>{"service": "landscape-service"}</customFields>
        </encoder>
      </appender>
    </then>
  </if>


  <root level="TRACE">
    <appender-ref ref="CONSOLE" />
    <if condition='p("LOGSTASH_ENABLED").equals("true")'>
      <then>
        <appender-ref ref="LOGSTASH_EVENT" />
      </then>
    </if>
  </root>
</configuration>
