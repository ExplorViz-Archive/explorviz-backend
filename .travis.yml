language: java

# jdk:
# - openjdk8
# - oraclejdk8

services:
  - mongodb
  - docker

jobs:
  fast_finish: true
  allow_failures:
    - jdk: oraclejdk8
  include:
    - jdk: openjdk8
    - jdk: oraclejdk8
    - stage: deploy
      jdk: openjdk8
    - stage: deploy
      jdk: openjdk8
      provider: script
      script: ls -la analysis

before_script:
  - chmod +x .travis/push.sh
  - chmod +x .travis/javadoc-push.sh

after_success:
  - .travis/javadoc-push.sh
  - .travis/push.sh

branches:
  only:
  - master
  - dev-1
